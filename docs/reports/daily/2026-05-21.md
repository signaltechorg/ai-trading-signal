# Daily Report — 2026-05-21

## What changed today
- Replaced the placeholder exposure math on `/allocation` with live gross/net exposure from `/api/widget/portfolio`.
- Expanded `/api/widget/portfolio` to return gross exposure, net exposure, and open-position counts for the allocation view.
- Tightened signal outcome handling so historical rows preserve explicit targets (`TP1`/`TP2`/`TP3`/`SL`/`expired`) and pending resolution skips rows missing TP/SL.
- Updated MAE/outcome tests to lock in the new target metadata behavior.
- Recorded the allocation work in `DAILY_INTEL_LOG.md` and refreshed `STATE.yaml`'s timestamp.

## What Hermes delegated
- No separate coding delegation was needed for this reporting pass; I inspected the repo state directly and verified the current workset.

## Verified
- `npm run build` passed.
- Targeted Jest coverage passed for `signal-history-pending`, `signal-history-mae`, and `signal-outcome`.
- `python3 -m py_compile scripts/scanner-engine.py` passed.

## Most important next action
- Triage the remaining scanner/error-log churn and then pick the next highest-leverage unresolved trust or conversion surface from the daily intel queue.
