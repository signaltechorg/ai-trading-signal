# Daily Report — 2026-05-20

## What changed today
- Landed the pro research pipeline update: `/api/pro/research` now enqueues jobs asynchronously, `/api/pro/research/[id]` is session-scoped, and `/api/cron/pro/research` drains queued jobs with lease/backoff handling and a TradingAgents bridge fallback.
- Tightened signal-history/outcome handling: timeframe-aware resolution, LEGACY backfill, and resolved-first/pending sorting cleanup to keep the tracker aligned with the emitted bar size.
- Closed the pro-audit follow-up on pricing/trial UX: pricing now shows all-time track-record stats, trial reminders include missed Pro P&L, and the Elite waitlist / willingness-to-pay capture is live.
- Fixed `scripts/scanner-engine.py` runtime scanning for crypto/forex/cfd by switching to the TradingView helper queries and regex name filtering; the direct smoke run now exits clean with 0 errors.

## What Hermes delegated
- Delegated long-running research execution to the new queue + cron drain path so user requests return immediately.
- No separate review pass was needed beyond the existing automation cycle.

## Most important next action
- Re-run the signal cron on schedule and watch for any regressions in the scanner output.
