# Daily Improvement — 2026-05-14

- **Chosen bottleneck:** Logged-out users hitting locked TP2/TP3 rows had a weak Pro conversion path. The fallback sent them to `/signin` and then back to `/pricing` without a checkout resume hint, so they had to hunt for the trial CTA again.
- **Change made:** Added `buildLockedTPFallbackHref()` in `apps/web/components/LockedTP.tsx` so the fallback now points to `/pricing?resume=checkout&interval=monthly&from=...`. Also simplified the unauthenticated redirect to `/signin?next=<resume-pricing-url>`, preserving the auto-resume checkout funnel. Added a unit test for the helper.
- **Verification run:** `npx jest apps/web/components/LockedTP.test.ts --runInBand`
- **Result:** Passed. The helper now generates the resume-aware pricing URL for TP2/TP3 upgrade clicks.
- **Next move:** Extend the same resume-aware flow to any other Pro CTAs that still bounce logged-out users to `/signin` without a checkout resume hint.
