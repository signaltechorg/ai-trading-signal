# Daily Improvement — 2026-05-15

- **Chosen bottleneck:** Supabase onboarding was still slightly brittle for self-hosters: the schema step could be misread, the migration step needed clearer env guidance, and the docs had a duplicated deploy section.
- **Change made:** Fixed the `/supabase` quick-start to use a valid Postgres connection-string template for the CLI path, aligned the migration step with `NEXT_PUBLIC_SUPABASE_URL` + service-role usage, and cleaned `docs/SUPABASE.md` to remove the duplicate Railway block and clarify Railway/Vercel setup.
- **Verification run:** `npm run build` in `apps/web`
- **Result:** Passed. The Supabase setup hub now gives a cleaner, less error-prone path from file-backed JSON to Supabase.
- **Next move:** Use the improved setup guide to drive the remaining runtime migration work for the JSON-backed stores, then validate a real end-to-end Supabase-backed deployment.
