## 2026-05-22
1. [Fix] Add an alert diagnostics panel to `apps/web/app/alerts/AlertsClient.tsx` showing the last matched signal, last delivery result, and channel permission state, so `/alerts` stops feeling opaque when rules do not fire. — source: `AUDIT_REPORT.md` still marks `/alerts` as partial and needing full verification
2. [Fix] Add fixture-backed golden-run presets plus a visible result checksum in `apps/web/app/backtest/BacktestClient.tsx`, so the complex `/backtest` TA surface can be regression-checked without manually recreating the same scenario every time. — source: `AUDIT_REPORT.md` still marks `/backtest` as partial and needing full verification
3. [Fix] Replace the hard `tradingview_screener` imports at `scripts/scanner-engine.py:23` with a startup capability probe and degraded-status banner on `apps/web/app/status/StatusClient.tsx`, so the May 21 import crash fails loudly and gracefully instead of silently breaking the scanner loop. — source: `scripts/signal-errors.log` shows repeated `ImportError: cannot import name 'cfd'` failures on 2026-05-21
4. [UX] Add a first-run quick-actions strip in `apps/web/app/copilot/CopilotClient.tsx` linking to `/leaderboard`, `/chrome-extension`, and `/api-keys`, so the new public copilot surface shipped in commit `c1c9bf96` guides users into the best next actions instead of dropping them into a blank prompt. — source: git log commit `c1c9bf96` (`feat(acquisition): add public leaderboard, copilot and chrome extension`)
