# TradeClaw Daily Intel — Idea Log

Append-only. Cron job `a2e2195c-f133-4e0e-90a0-fc8acb559d79` reads the last 14 days before proposing new ideas to avoid repetition.

## 2026-04-18
1. [UX] Add "why this signal" explainer next to each card, especially when signal flips
2. [Performance] Make backtests and calibration incremental — PR checks rerun only changed symbols/timeframes
3. [Growth] Ship live demo with seeded sample data and loud star CTA on first screen
4. [Fix] Replace global ATR stop with per-symbol calibration from outcome history (gold, FX, BTC differ)
5. [Feature] Shareable signal cards with permalink, chart snapshot, and Telegram/Discord-ready embed
## 2026-04-20
1. [Feature] Add a `/supabase` migration hub with `docs/SUPABASE.md`, `schema.sql`, and env examples for the new Postgres-backed data surfaces
2. [Feature] Extend `/screener`, `/dashboard`, and `/portfolio` with S&P 500 top-20 symbol support
3. [Growth] Ship `packages/tradeclaw-extension/manifest.json` plus a `/chrome-extension` install page for live signal badges
4. [Fix] Show an explicit outage state and last-success timestamp on `/news` when `/api/news` falls back
5. [UX] Add scanner health, last run, and last failure details to `/status` for `scripts/scanner-engine.py`

## 2026-04-30
1. [Growth] Launch `/strategies/leaderboard` so users can submit strategy JSON, auto-backtest it server-side, and share a public Sharpe-ranked board—this turns community experiments into viral proof instead of isolated local runs. — source: STATE.yaml TC-164 pending
2. [Feature] Ship `/copilot` as a no-LLM signal assistant that answers from live `/api/signals` data with inline card previews, giving first-time visitors a guided way to explore which pair looks strongest without reading the whole dashboard. — source: STATE.yaml TC-165 pending
3. [UX] Add a “last matched signal / last delivery result” debug panel on `/alerts` so users can see why a rule fired or stayed quiet without opening server logs. — source: AUDIT_REPORT.md flags `/alerts` as partial and still needing full verification
4. [Fix] Add a deterministic fixture mode on `/backtest` that locks candles and inputs, then shows a shareable run checksum beside results so users can compare indicator math across machines without guessing. — source: AUDIT_REPORT.md flags `/backtest` as partial due to complex TA verification
5. [Fix] Add a “Canonical scheduler: system cron” badge with last-success timestamp on `/status` so policy-blocked agent exec attempts stop reading like real scanner outages. — source: scripts/signal-errors.log entries on 2026-04-29 show false-failure noise after the system cron was already healthy

## 2026-05-01
1. [Growth] Turn the existing `apps/web/lib/asset-requests.ts` API into a public `/asset-requests` page where visitors can vote for SOLUSD/NVDA/USOIL and subscribe for launch alerts, so discussion #30 becomes structured demand instead of dead comments. — source: GitHub discussion #30 + existing `/api/asset-requests*` routes with no user-facing page
2. [Feature] Add one-click MT5/OANDA/Alpaca webhook templates plus a “send test payload” flow inside `apps/web/app/brokers/BrokersClient.tsx` and `/settings/webhooks`, so the broker demand in discussion #27 turns into an actual handoff surface instead of docs-only cards. — source: GitHub discussion #27 broker integration requests
3. [UX] Split `/proof` and `/accuracy` into “Real tracked” vs “Simulated demo” tabs and show expectancy/max drawdown beside win rate, so skeptical users land on audited metrics first instead of having to infer what is seeded. — source: GitHub discussion #31 on accuracy methodology + `apps/web/app/proof/ProofClient.tsx` and `apps/web/app/accuracy/AccuracyClient.tsx`
4. [Fix] Add TradingView-provider cooldown/backoff in `scripts/scanner-engine.py` and expose the active upstream-degraded state on `/status`, so repeated 429 storms stop hammering all symbols silently and users can see when the issue is provider-side. — source: `scripts/signal-errors.log` shows repeated 2026-04-02 TradingView 429 failures across all symbols

## 2026-05-02
1. [Feature] Ship `/pilot` dashboard surfacing the Binance Futures executor state — open positions, day/week realized PnL against kill-switch thresholds, blocked-pyramid attempts, TP1 partial-close events — so the gates added in `a45384b0/bc6bcf4c/c29ef2e8/3bbbb4ec` are visible instead of server-side-only. — source: pilot commits landed without a UI surface
2. [Performance] Wrap the new Pro group broadcaster from `3bb53091/a5ed1c67` with Telegram 429-aware backoff plus a persistent retry queue, then expose broadcast queue depth and last-broadcast latency on `/status`, so a Pro signal burst during Telegram throttling does not silently drop alerts. — source: commits 3bb53091 + a5ed1c67 ship Pro broadcast with no observable rate-limit handling
3. [Feature] Add a `GET /api/metrics` Prometheus exposition (signal cadence, broadcast queue depth, pilot exec count, Stripe active-Pro count, MRR) — closes the still-open issue #19 from 2026-03-27. — source: GitHub issue #19
4. [Growth] Use the per-channel platform routing introduced in `1694a59d` to add a Discord bot variant of the Pro signal broadcaster — closes issue #38 (open since 2026-04-01) and matches the broker-channel demand in discussion #27. — source: GitHub issue #38 + commit 1694a59d
5. [Fix] Add an outcome-resolution heartbeat tile on `/status` plus a staleness banner on `/track-record` and `/leaderboard` that fires when `signal_history` rows have not been resolved in >15 min — Writer B (cron `/api/cron/signals` `resolveOldSignals`) is the only resolver per workspace CLAUDE.md, so equity and win-rate go silently stale if it stalls. — source: workspace CLAUDE.md TradeClaw signal-architecture section

## 2026-05-03
1. [Fix] Deep-link the past-due banner straight to Stripe's card-update form via `billing_portal.sessions.create` with `flow_data: { type: 'payment_method_update', after_completion: { type: 'redirect', redirect: { return_url: ... } } }`, so dunning users land on the card form instead of the portal home. — source: commit ccbc998c added the banner with a generic portal CTA; Stripe portal `flow_data` API
2. [Growth] Add a T-3d trial-ending email variant alongside the T-1d one in `apps/web/lib/trial-end-email.ts`, listing the top 3 Pro-only signals the user has consumed during trial (with realized P&L if paper-traded), so the cancel decision is anchored to concrete missed P&L instead of just a countdown. — source: commit d390d50b shipped only the T-1d reminder; trial-end logic in same file
3. [Feature] Promote the admin `scripts/resend-pro-invites.ts` flow into a self-serve "rejoin Pro Telegram group" button on `/dashboard/billing` for active-Pro users, gated by Stripe-tier check + 1/hr rate limit, so kicked-then-rejoined users do not have to DM support. — source: commit a99fda70 ("script for fixing broken group invites") implies recurring breakage that today requires manual intervention
4. [UX] Recompute and cache the resolved outcome on `/api/og/signal/[id]` so OG cards shared 4h+ after entry render the TP1/TP2/SL state from `classifySignalOutcome` instead of the stale entry-time confidence — old screenshots stay accurate proof instead of going contrarian when a signal stops out. — source: commit 182fd918 added live TP/SL outcome badge on dashboard cards but did not propagate to OG image
5. [Fix] Add a `/admin/stripe-events` table that reads the idempotent webhook ledger from `2f141171`, filterable by customer email, with last 14d of `event_id`/`type`/`status`, so "I paid but I'm still on free" tickets resolve in one click without psql access. — source: commit 2f141171 added the ledger server-side only; admin dashboard scaffolding from 8506b457

## 2026-05-04
1. [UX] Add a browser-push diagnostics panel to `apps/web/app/settings/alerts/page.tsx` and the onboarding alert step so users can see permission state, subscription age, and last successful push instead of guessing whether web alerts are armed. — source: commits `fcebb831` (real-time web push via VAPID) + `458aa556` (onboarding step 3 enables browser alerts)
2. [Feature] Add a billing lifecycle timeline on `/dashboard/billing` that shows `trial_end`, dunning email sent, past-due grace expiry, and current Telegram invite status in one card, so users understand exactly why access changed before they churn. — source: commits `d390d50b`, `ccbc998c`, `4d684455`, and `086c5ff0`
3. [Fix] Persist Telegram access diagnostics on the user-facing billing/onboarding surface — last `sendInvite` attempt, `chat_not_found` / `bot_blocked`, and retry CTA — instead of surfacing those states only in transient send paths. — source: commits `5bf72a12` + `e5dff155`
4. [Growth] Add a “share this week’s pulse” export on `apps/web/app/report` that renders the same stats as GitHub discussion #65 into a Telegram/X/Discord-ready image, turning the weekly report into a distribution asset instead of a GitHub-only update. — source: discussion #65 + existing `/report` surface
5. [Fix] Add a `/admin/cron-health` fanout target checker that shows localhost vs public-host target, last sub-cron latency, and mismatch warnings, so the `127.0.0.1` regression fixed in `01f3066c` is caught before cron fanout silently misses jobs. — source: commit `01f3066c`


## 2026-05-05
1. [Feature] Ship `apps/web/app/supabase/page.tsx` with a production migration checklist, env verifier, and copy-ready `schema.sql` / `docs/SUPABASE.md` links so self-hosters can move off file JSON without reading repo internals first. — source: `STATE.yaml` pending `TC-163`
2. [Feature] Build `packages/tradeclaw-extension/manifest.json` plus popup/options UI and a user-facing `/chrome-extension` install page, so the still-pending browser surface becomes a passive acquisition channel instead of another internal TODO. — source: `STATE.yaml` pending `TC-166`
3. [Fix] Add an explicit upstream-outage state to `apps/web/app/news/NewsClient.tsx` showing last successful refresh time and degraded-data copy when `/api/news` falls back, because `/news` currently fails too quietly for users to trust it. — source: `AUDIT_REPORT.md` unresolved `/news` item
4. [Fix] Add Vitest coverage around `apps/web/app/lib/signal-generator.ts` and `apps/web/lib/tracked-signals.ts` with seeded candle fixtures for confluence, TP/SL resolution, and tier gating, so future signal-engine changes stop shipping without a safety net. — source: GitHub issue `#17` + 14-day git log shows heavy signal churn without matching test growth
5. [UX] Surface the new `signal_run_log` rows in an `/admin/signal-runs` view (or `/status` subpanel) filtered by writer/result/latency, so operators can inspect cron behavior from the UI instead of querying the database after each anomaly. — source: commit `1cf37354` (`feat(signals): cron writes signal_run_log audit row each run`)

## 2026-05-06
1. [Fix] Add a `apps/web/__tests__/tier-leak.spec.ts` snapshot guard that asserts the free-tier response from `/api/v1/signals` and `filterSignalByTier` never contains `stopLoss`, `takeProfit*`, `entry`, `rsi`, or `ema*` keys, so the leaks fixed in `bb98cadf` cannot silently regress on the next refactor. — source: commit `bb98cadf` ("implement signal visibility controls and fix free/pro data leaks") + `docs/audits/free-vs-pro-2026-05-05.md` findings register
2. [Feature] Build `/admin/signal-runs` reading the last 100 rows of `signal_run_log` (run_at, generated count, dedup-skipped, resolver count, error blob) added in `1cf37354`, so cron health and outcome-resolver throughput are inspectable without `psql $DATABASE_URL` — pairs with the heartbeat tile already proposed for `/status`. — source: commit `1cf37354` ("cron writes signal_run_log audit row each run") + migration `dabfd1c8`
3. [UX] Add `/admin/symbol-health` listing each tracked symbol's primary data source, active proxy (e.g. BNO for Brent), last successful candle ts, and any synthetic-fallback warning, so the kind of silent proxy swap done in `f5c3ef8b` is observable before users notice missing signals. — source: commit `f5c3ef8b` ("swap Brent CFD for BNO ETF proxy") + workspace CLAUDE.md note on synthetic fallback in `ohlcv.ts`
4. [Fix] Implement a `/api/csp-report` collector and an `/admin/csp` viewer that aggregates top violated directives from the last 7 days, so the `Content-Security-Policy-Report-Only` header added in `3d4140c4` actually feeds remediation and we can flip CSP from Report-Only to enforced. — source: commit `3d4140c4` ("add HSTS and CSP-Report-Only response headers") with no consuming endpoint yet
5. [Performance] Memoize `calculateRSI/MACD/EMA/BB/Stoch/ADX` results in `apps/web/app/lib/ta-engine.ts` keyed by `(symbol, timeframe, lastCandleCloseTs)` so Writer A (request side-effect from `/api/signals`, dashboard SSR) and Writer B (`/api/cron/signals` 5-min job) stop recomputing the same indicators on the same closed candle three+ times per cycle. — source: workspace CLAUDE.md two-writer signal-architecture section + `apps/web/app/lib/signal-generator.ts` calling into `ta-engine.ts` per request

## 2026-05-11
1. [Fix] Replace the mock data in `apps/web/app/execution/ExecutionClient.tsx` with real `/api/v1/execution` and `/api/v1/broker` reads, so the public execution log stops showing fake Alpaca fills after the new live execution work landed. — source: `apps/web/app/execution/ExecutionClient.tsx` TODO + discussion #27 + commits `a45384b0`, `8334cbe3`, `9768d524`
2. [Fix] Wire `apps/web/app/risk/RiskClient.tsx` to live breaker, equity, and veto telemetry instead of seeded demo values, so users can see why kill-switches or vetoes fired after the recent execution safety changes. — source: `apps/web/app/risk/RiskClient.tsx` TODO + commits `bc6bcf4c`, `c29ef2e8`, `4c001807`
3. [Feature] Add an `/admin/audit-log` viewer and a tile from `apps/web/app/admin/page.tsx`, reading the new `admin_audit_log` table so Pro grants and social approvals are inspectable instead of append-only writes with no UI. — source: `apps/web/migrations/028_admin_audit_log.sql` + commits `443c2b05`, `0fc6de63`
4. [UX] Turn `apps/web/app/brokers/BrokersClient.tsx` into a first-run setup checklist with API-key status, sandbox/live mode, and a “send test order” path into `/execution`, because discussion #27 asks for broker integrations but the current surface is still mostly docs and snippets. — source: discussion #27 + `apps/web/app/brokers/BrokersClient.tsx`
5. [Growth] Surface `DataProvenanceBadge` on `/dashboard` signal cards and the landing hero so visitors can see whether numbers are live, mixed, or simulated before trusting them, instead of limiting provenance cues to the accuracy pages. — source: `apps/web/components/data-provenance-badge.tsx` + current usage only in `apps/web/app/accuracy/AccuracyClient.tsx` and `apps/web/app/components/accuracy-stats-bar.tsx`

## 2026-05-13
1. [Fix] Wire `apps/web/app/allocation/AllocationClient.tsx` to a real portfolio exposure endpoint so Current Exposure and Headroom stop using placeholder math and reflect live open-position risk. — source: `apps/web/app/allocation/AllocationClient.tsx` TODO
2. [Feature] Replace the disabled “Add to Slack” block in `apps/web/app/slack/SlackClient.tsx` with real Slack OAuth install plus channel handshake, so Slack stops being docs-only while the page itself promises one-click workspace install. — source: `apps/web/app/slack/SlackClient.tsx` placeholder + Coming Soon badge
3. [Growth] Turn the Elite “Coming Soon” work from `apps/web/migrations/030_elite_interest.sql` and commit `2aee701c` into a public `/elite` waitlist page that captures broker, AUM band, and execution preferences before launch, so pricing curiosity becomes structured demand instead of a dead badge. — source: commit `2aee701c` + `apps/web/migrations/030_elite_interest.sql`
4. [UX] Make `/api-usage` tier-aware by replacing the static Pro “Coming Soon” card in `apps/web/app/api-usage/APIUsageClient.tsx` with the user’s real quota, reset window, and upgrade gate, so developers can see whether higher limits are actually available instead of reading a placeholder. — source: `apps/web/app/api-usage/APIUsageClient.tsx`

## 2026-05-14
1. [Growth] Build `/admin/conversion-attribution` reading the `?from=<surface>` UTM tags shipped in commit `5d18e8e7` and joining against Stripe `checkout.session.completed` events, so we can see which upgrade surfaces (LockedTP, navbar, dashboard banner, signal cards) actually convert and prune the dead ones instead of guessing. — source: commit `5d18e8e7` ("tag every upgrade CTA with ?from=<surface>") with no readout surface yet
2. [Feature] Make the new premium-band equity filter from commit `1f91dfe8` shareable via a `?band=premium` URL param on `/track-record` and propagate it into `/embed/track-record`, so traders pitching the Pro tier can deep-link the premium-band-only proof instead of asking viewers to re-toggle the filter. — source: commit `1f91dfe8` ("add premium band filtering to equity curve and track record page")
3. [Fix] Surface the new `exit_price` snapshot from commit `4e501077` on closed positions in `/paper-trading` and `/execution` with realized R and slippage-vs-TP delta, so users see *why* a position closed (TP hit, SL hit, manual exit) instead of seeing only the entry-time projection. — source: commit `4e501077` ("record exit price snapshot on position close") + no current UI consumer
4. [Feature] Add a Telegram alert webhook on the new `/admin/ops` dashboard from commit `63ec44d6` that fires when signal-cadence stalls >15min, outcome-resolution lags >30min, or broadcast queue depth >20, so the observability we just shipped pages someone instead of waiting for a manual check. — source: commit `63ec44d6` ("add ops dashboard for signal engine observability") with no alerting layer
5. [Growth] Wire client-side CSV export to the `/screener` results header generating `tradeclaw-screener-YYYY-MM-DD.csv` with Pair/Signal/Strength/RSI/MACD/Entry/TP/SL columns, so traders pipe screener output into their own sheet workflows without scraping the DOM and we close a 6-week-old "good first issue". — source: GitHub issue #7 (open since 2026-03-27, not in past 14d of intel log)
## 2026-05-16
1. [Fix] Teach `apps/web/app/lib/signal-generator.ts` to use per-symbol ATR stop profiles learned from tracked outcomes, then expose the active stop profile on `/signal/[id]` so XAUUSD/BTCUSD/EURUSD stops stop feeling one-size-fits-all. — source: GitHub issue #53 + pending issue state shows calibration still open
2. [Feature] Add Email as a first-class channel in `apps/web/app/settings/alerts/page.tsx` with a matching `/api/email/alerts` send path, so users who do not want Telegram/Discord/web push can still receive live signal notifications. — source: GitHub issue #20 + current alerts surface only covers non-email channels
3. [Feature] Embed a live TradingView chart panel on `apps/web/app/signal/[id]/page.tsx` (or its client component) with the current pair/timeframe preloaded, so users can inspect the setup visually without leaving TradeClaw. — source: GitHub issue #37 requesting TradingView chart widget integration
4. [Growth] Ship real Malay and Arabic entry points (`/ms`, `/ar`) plus locale links on the landing/docs nav, so TradeClaw can convert the regional/self-hosting audience instead of staying English-only. — source: GitHub issue #16 + current page audit only lists `/es` and `/zh` as localized routes
5. [Performance] Add `.github/workflows/backtest-report.yml` that runs a fixed-fixture backtest on every PR and comments the delta in win rate / expectancy / drawdown, so indicator changes stop merging blind and contributors get instant trust signals. — source: GitHub issue #40 + `git log --since="14 days ago"` shows heavy signal-engine churn without an automated PR backtest check

## 2026-05-17
1. [Growth] Turn GitHub issue #78 Topic 1 into a real `apps/web/app/blog` post that links `/calibration` and the ATR logic in `apps/web/app/lib/ta-engine.ts`, so the still-open per-symbol stop-calibration work starts pulling SEO traffic before issue #53 ships. — source: GitHub issue #78 Topic 1 + issue #53
2. [Growth] Publish the honest TradeClaw-vs-TradingView article from issue #78 under `apps/web/app/blog` and route readers into `/vs-tradingview`, `/pricing`, and `/star`, so the comparison surface becomes an acquisition funnel instead of a static feature page. — source: GitHub issue #78 Topic 2 + existing `/vs-tradingview` route
3. [Growth] Write the Stripe-plus-Telegram sync teardown in `apps/web/app/blog`, citing the recent bot and webhook fixes, so the billing reliability work turns into developer-trust content rather than invisible plumbing. — source: GitHub issue #78 Topic 3 + commits `18c867d8`, `77d86d29`, `7e1d5478`
4. [Feature] Build `/operator/connectors` backed by `/api/operator/connectors` with uptime badges, last-check timestamps, and a manual re-check action, so the new operator layer exposes exchange, market-data-hub, and Telegram bot health before failures spill into user-facing surfaces. — source: `STATE.yaml` TC-173 planned
5. [Feature] Ship `/operator/insights` cards that flag pair/timeframe accuracy drift from tracked outcomes and `signal_run_log`, so operators get actionable recommendations instead of raw observability only. — source: `STATE.yaml` TC-174 planned + commit `1cf37354`
6. [Feature] Finish the connector dashboard polish by adding summary pills, expandable error logs, and a clearer re-check control on `/admin/operator/connectors`, so the operator command center surfaces failures faster for admins. — source: current `TC-173` implementation cycle

## 2026-05-21
1. [Fix] Replace the placeholder exposure math in `apps/web/app/allocation/AllocationClient.tsx` with live gross/net exposure from `/api/widget/portfolio`, so the Current Exposure and Headroom cards track the paper-trading snapshot instead of a fake 60% fill; verified with `npm run build`. — source: `apps/web/app/allocation/AllocationClient.tsx` TODO
2. [Growth] Publish the honest TradeClaw-vs-TradingView article under `apps/web/content/blog` and route readers into `/vs-tradingview`, `/pricing`, and `/star`, so the comparison keyword surface becomes an acquisition funnel instead of a static page. — source: `apps/web/content/blog/BACKLOG.md` + existing `/vs-tradingview` route

## 2026-05-22
1. [Growth] Verify the new leaderboard freshness cue and TradeClaw-vs-TradingView comparison post are shipping cleanly, with the public `/leaderboard` heartbeat, `/news` refresh health copy, and the blog route all passing `npm run build` plus the targeted `signal-outcome` Jest suite. — source: current working tree changes in `apps/web/app/leaderboard/LeaderboardClient.tsx`, `apps/web/app/news/NewsClient.tsx`, `apps/web/content/blog/tradeclaw-vs-tradingview.mdx`, and `apps/web/lib/__tests__/signal-outcome.test.ts`
2. [Verify] Build and targeted test completed successfully: `npm test -- apps/web/lib/__tests__/signal-outcome.test.ts --runInBand` passed, and `npm run build` completed with only pre-existing Next.js warnings about duplicate manual mocks and NFT tracing.
## 2026-05-22
1. [Fix] Add an alert diagnostics panel to `apps/web/app/alerts/AlertsClient.tsx` showing the last matched signal, last delivery result, and channel permission state, so `/alerts` stops feeling opaque when rules do not fire. — source: `AUDIT_REPORT.md` still marks `/alerts` as partial and needing full verification
2. [Fix] Add fixture-backed golden-run presets plus a visible result checksum in `apps/web/app/backtest/BacktestClient.tsx`, so the complex `/backtest` TA surface can be regression-checked without manually recreating the same scenario every time. — source: `AUDIT_REPORT.md` still marks `/backtest` as partial and needing full verification
3. [Fix] Replace the hard `tradingview_screener` imports at `scripts/scanner-engine.py:23` with a startup capability probe and degraded-status banner on `apps/web/app/status/StatusClient.tsx`, so the May 21 import crash fails loudly and gracefully instead of silently breaking the scanner loop. — source: `scripts/signal-errors.log` shows repeated `ImportError: cannot import name 'cfd'` failures on 2026-05-21
4. [UX] Add a first-run quick-actions strip in `apps/web/app/copilot/CopilotClient.tsx` linking to `/leaderboard`, `/chrome-extension`, and `/api-keys`, so the new public copilot surface shipped in commit `c1c9bf96` guides users into the best next actions instead of dropping them into a blank prompt. — source: git log commit `c1c9bf96` (`feat(acquisition): add public leaderboard, copilot and chrome extension`)

## 2026-05-30
1. [Growth] Replace the `TODO:` placeholders in `apps/web/content/blog/draft-2026-05-25.mdx` and publish it under a real slug, so the new Journal surface gets a launch post on `/blog` instead of sitting in a draft PR. — source: GitHub issue `#81` + draft PR `#82`
2. [Growth] Publish the PostgreSQL persistence deep dive from the week-22 editorial backlog, tying `feat(operator-memory)` to self-hosting setup docs and the Operator dashboard, so durable storage becomes a clear value prop instead of an invisible backend improvement. — source: GitHub issue `#81` topic 3
3. [Growth] Add a “Join this week’s discussion” module on `/report` that links to the current GitHub Weekly Pulse thread, so the live stats page turns into a conversation funnel instead of a dead-end status surface. — source: GitHub discussion `#83` (`📊 Weekly Pulse — Week 22, 2026`)

## 2026-05-31
1. [Growth] Turn `apps/web/content/blog/draft-2026-06-01.mdx` into the shipped “Announcing the TradeClaw Python SDK: pip install tradeclaw” post and add a CTA from `/api-keys`, so Python algo traders can move from key creation to typed SDK usage instead of hand-writing REST calls. — source: GitHub issue #85 Topic 1 + draft scaffold PR mention
2. [Growth] Add a Telegram premium setup guide entry point on `apps/web/app/telegram/page.tsx` that deep-links into billing and channel join flow, so the TC-202/203/204 premium-channel work becomes a searchable onboarding asset instead of a hidden feature. — source: GitHub issue #85 Topic 2 + shipped TC-202/203/204 Telegram premium channel work
3. [Growth] Publish a comparison post sourced from `apps/web/app/compare/page.tsx` (or its content files) explaining when to pick TradeClaw vs TA-Lib vs pandas-ta, so the existing `/compare` matrix starts capturing search traffic instead of only serving on-site visitors. — source: GitHub issue #85 Topic 3 + existing `/compare` surface
4. [Growth] Add a “Start contributing” empty-state card on `apps/web/app/report/page.tsx` when the weekly pulse shows `Active Contributors = 0`, linking straight to `/contribute` and selected good-first issues, so the public report turns a weak community metric into a recruitment funnel. — source: GitHub discussion #83 weekly pulse shows 0 active contributors

## 2026-06-01
1. [Feature] Add a notebook-ready Python tab in `apps/web/app/api-keys/ApiKeysClient.tsx` that uses the just-created key to show pandas/polars examples, `tc.quota()`, and a typed SDK call flow, so issue #85 Topic 1 turns into live developer onboarding instead of generic REST copy. — source: GitHub issue #85 Topic 1 + apps/web/app/api-keys/ApiKeysClient.tsx
2. [UX] Add copyable side-by-side code tabs in `apps/web/app/compare/CompareClient.tsx` for TradeClaw vs TA-Lib vs pandas-ta plus a plain-English “who should pick this?” verdict card, so issue #85 Topic 3 stops being a static matrix and becomes a developer decision tool. — source: GitHub issue #85 Topic 3 + apps/web/app/compare/CompareClient.tsx
3. [Growth] Add a contributors drilldown card in `apps/web/app/report/ReportClient.tsx` that names the 2 active contributors, links to their merged work, and points readers to `/contribute`, so discussion #95 turns “Active Contributors: 2” into social proof and a recruiting funnel instead of a lonely stat. — source: GitHub discussion #95 + apps/web/app/report/ReportClient.tsx
4. [Feature] Add a TradingView profile/preset builder to `apps/web/app/tradingview-alerts/TVAlertsClient.tsx` that outputs prefilled webhook JSON per symbol/strategy, so the `STATE.yaml` “TradingView profile integration” next action becomes a concrete setup flow instead of manual copy-paste. — source: STATE.yaml next_actions + apps/web/app/tradingview-alerts/TVAlertsClient.tsx
5. [Feature] Split the webhook marketplace into provider cards with “works now / planned” filters and per-provider setup status in `apps/web/components/marketplace-client.tsx`, so the multi-provider marketplace scaffold called out in `STATE.yaml` becomes a visible product surface instead of a hidden roadmap line. — source: STATE.yaml next_actions + apps/web/components/marketplace-client.tsx
