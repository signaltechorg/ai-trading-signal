global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: tradeclaw
    metrics_path: /api/metrics
    static_configs:
      - targets:
          - host.docker.internal:3000
    params:
      tf: ["H1"]
